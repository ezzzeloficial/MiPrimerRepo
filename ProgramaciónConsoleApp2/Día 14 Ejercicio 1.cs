using System;

class Program
{
    static void Main(string[] args)
    {
        int edad = 21; // Declaramos una variable llamada 'edad' y le asignamos el valor 21.
        double altura = 1.77; // Declaramos una variable llamada 'altura' y le asignamos el valor 1.77.
        string nombre = "Ezel"; // Declaramos una variable llamada 'nombre' y le asignamos el valor "Ezel".

        // Imprimimos las variables en la consola.
        Console.WriteLine("Mi nombre es " + nombre);
        Console.WriteLine("Tengo " + edad + " años");
        Console.WriteLine("Mido " + altura + " metros");
    }
}